apiVersion: v1
kind: Service

metadata:
  name: book-catalog-service
  namespace: library-system

  labels:
    app: book-catalog-service

spec:

  type: NodePort

  selector:
    app: book-catalog-service

  ports:

    - name: http

      port: 8080

      targetPort: 8080